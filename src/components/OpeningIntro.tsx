import React from 'react';

// The old timed splash screen interrupted visitors before they reached the story.
// Keeping the component as a no-op avoids disturbing the existing page composition.
export const OpeningIntro: React.FC = () => null;
